@extends('layouts.app')
@section('content')
<div class="mb-8"><p class="eyebrow">{{ __('common.analytics') }}</p><h1 class="page-title">{{ __('common.branch_reports') }}</h1><p class="page-subtitle">{{ __('common.branch_reports_subtitle') }}</p></div>
<form method="GET" class="card mb-6"><div class="grid gap-4 md:grid-cols-3"><div><label class="form-label">{{ __('common.date_from') }}</label><input class="form-input" type="date" name="date_from" value="{{ $filters['date_from']??'' }}"></div><div><label class="form-label">{{ __('common.date_to') }}</label><input class="form-input" type="date" name="date_to" value="{{ $filters['date_to']??'' }}"></div><div><label class="form-label">{{ __('common.branch') }}</label><select class="form-input min-h-28" multiple name="branch_ids[]">@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected(in_array($branch->id,$filters['branch_ids']??[]))>{{ $branch->name }}</option>@endforeach</select></div></div><div class="mt-5 flex gap-3"><button class="btn-primary">{{ __('common.apply_filters') }}</button><a class="btn-secondary" href="{{ route('reports.branches') }}">{{ __('common.reset') }}</a></div></form>
<div class="card overflow-hidden p-0"><table class="data-table"><thead><tr><th>{{ __('common.date') }}</th><th>{{ __('common.branch') }}</th><th>{{ __('common.complaints') }}</th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ $row->complaint_date }}</td><td>{{ $row->branch?->name }}</td><td class="font-semibold">{{ $row->total }}</td></tr>@empty<tr><td colspan="3" class="empty">{{ __('common.no_data') }}</td></tr>@endforelse</tbody></table></div>
@endsection
