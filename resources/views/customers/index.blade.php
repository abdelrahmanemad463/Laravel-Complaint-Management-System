@extends('layouts.app')
@section('content')
<div class="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p class="eyebrow">{{ __('common.support') }}</p><h1 class="page-title">{{ __('common.customers') }}</h1></div>@can('customer.create')<a href="{{ route('customers.create') }}" class="btn-primary">{{ __('common.new_customer') }}</a>@endcan</div>
<form class="card mb-6 flex flex-wrap gap-3" method="GET"><input class="form-input max-w-lg flex-1" name="search" value="{{ $search }}" placeholder="{{ __('common.search_customer') }}"><button class="btn-primary">{{ __('common.search') }}</button><a href="{{ route('customers.index') }}" class="btn-secondary">{{ __('common.reset') }}</a></form>
<div class="card overflow-hidden p-0"><div class="border-b px-6 py-4 text-sm text-slate-500">{{ $customers->total() }} {{ __('common.customers_found') }}</div><div class="overflow-x-auto"><table class="data-table"><thead><tr><th>{{ __('common.name') }}</th><th>{{ __('common.primary_phone') }}</th><th>{{ __('common.other_phones') }}</th><th>{{ __('common.complaints') }}</th><th></th></tr></thead><tbody>@forelse($customers as $customer)<tr><td><a class="font-semibold text-indigo-700 hover:underline" href="{{ route('customers.show',$customer) }}">{{ $customer->name }}</a></td><td>{{ $customer->phone_primary }}</td><td>{{ collect([$customer->phone_2,$customer->phone_3,$customer->phone_4])->filter()->join(', ') ?: '—' }}</td><td>{{ $customer->complaints_count }}</td><td class="text-end"><a class="btn-small" href="{{ route('customers.show',$customer) }}">{{ __('common.view') }}</a></td></tr>@empty<tr><td colspan="5" class="empty">{{ __('common.no_customers') }}</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $customers->links() }}</div></div>
@endsection
