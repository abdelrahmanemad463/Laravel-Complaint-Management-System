@extends('layouts.app')
@section('content')
<div class="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p class="eyebrow">{{ __('common.support') }}</p><h1 class="page-title">{{ __('common.customers') }}</h1></div>@can('customer.create')<a href="{{ route('customers.create') }}" class="btn-primary">{{ __('common.new_customer') }}</a>@endcan</div>
<form class="card mb-6 flex flex-col gap-3 sm:flex-row sm:flex-wrap p-4 sm:p-6" method="GET"><input class="form-input max-w-lg flex-1 min-w-[200px]" name="search" value="{{ $search }}" placeholder="{{ __('common.search_customer') }}"><button class="btn-primary min-h-[44px] w-full sm:w-auto">{{ __('common.search') }}</button><a href="{{ route('customers.index') }}" class="btn-secondary min-h-[44px] inline-flex items-center justify-center w-full sm:w-auto">{{ __('common.reset') }}</a></form>
<div class="card overflow-hidden p-0"><div class="border-b px-4 sm:px-6 py-4 text-sm text-slate-500">{{ $customers->total() }} {{ __('common.customers_found') }}</div><div class="overflow-x-auto -mx-3 sm:mx-0"><table class="data-table"><thead><tr><th class="px-3 sm:px-4 lg:px-6 text-xs sm:text-sm">{{ __('common.name') }}</th><th class="px-3 sm:px-4 lg:px-6 text-xs sm:text-sm">{{ __('common.primary_phone') }}</th><th class="px-3 sm:px-4 lg:px-6 text-xs sm:text-sm">{{ __('common.other_phones') }}</th><th class="px-3 sm:px-4 lg:px-6 text-xs sm:text-sm">{{ __('common.complaints') }}</th><th class="px-3 sm:px-4 lg:px-6"></th></tr></thead><tbody>@forelse($customers as $customer)<tr><td><a class="font-semibold text-indigo-700 hover:underline" href="{{ route('customers.show',$customer) }}">{{ $customer->name }}</a></td><td>{{ $customer->phone_primary }}</td><td>{{ collect([$customer->phone_2,$customer->phone_3,$customer->phone_4])->filter()->join(', ') ?: '—' }}</td><td>{{ $customer->complaints_count }}</td><td class="text-end"><a class="btn-small" href="{{ route('customers.show',$customer) }}">{{ __('common.view') }}</a></td></tr>@empty<tr><td colspan="5" class="empty">{{ __('common.no_customers') }}</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $customers->links() }}</div></div>
@endsection
