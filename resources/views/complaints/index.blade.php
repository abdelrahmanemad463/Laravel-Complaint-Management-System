@extends('layouts.app')
@section('content')
<div class="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p class="eyebrow">{{ __('common.support') }}</p><h1 class="page-title">{{ __('common.complaints') }}</h1><p class="page-subtitle">{{ $complaints->total() }} {{ __('common.results') }}</p></div>@can('complaint.create')<a href="{{ route('complaints.create') }}" class="btn-primary">{{ __('common.new_complaint') }}</a>@endcan</div>
@include('complaints.filters')
<div class="card overflow-hidden p-0"><div class="border-b px-4 sm:px-6 py-4 text-sm text-slate-500">{{ __('common.results') }}: <strong>{{ $complaints->total() }}</strong></div><div class="overflow-x-auto -mx-3 sm:mx-0"><table class="data-table"><thead><tr><th class="px-3 sm:px-4 text-xs sm:text-sm">ID</th><th class="px-3 sm:px-4 text-xs sm:text-sm">{{ __('common.customer') }}</th><th class="px-3 sm:px-4 text-xs sm:text-sm">{{ __('common.branch') }}</th><th class="px-3 sm:px-4 text-xs sm:text-sm">{{ __('common.service') }}</th><th class="px-3 sm:px-4 text-xs sm:text-sm">{{ __('common.category') }}</th><th class="px-3 sm:px-4 text-xs sm:text-sm">{{ __('common.type') }}</th><th class="px-3 sm:px-4 text-xs sm:text-sm">{{ __('common.priority') }}</th><th class="px-3 sm:px-4 text-xs sm:text-sm">{{ __('common.status') }}</th><th class="px-3 sm:px-4 text-xs sm:text-sm">{{ __('common.date') }}</th><th class="px-3 sm:px-4"></th></tr></thead><tbody>@forelse($complaints as $complaint)<tr><td>#{{ $complaint->id }}</td><td><a class="font-semibold text-indigo-700" href="{{ route('customers.show',$complaint->customer) }}">{{ $complaint->customer?->name }}</a></td><td>{{ $complaint->branch?->localized_name }}</td><td><span class="badge" style="--badge-color:{{ $complaint->service?->color }}">{{ $complaint->service?->localized_name }}</span></td><td>{{ $complaint->category?->localized_name }}</td><td>{{ $complaint->type?->localized_name }}</td><td><span class="badge" style="--badge-color:{{ $complaint->priority?->color }}">{{ $complaint->priority?->localized_name }}</span></td><td><span class="badge" style="--badge-color:{{ $complaint->status?->color }}">{{ $complaint->status?->localized_name }}</span></td><td>{{ optional($complaint->complaint_date)->format('Y-m-d') }}</td><td><a class="btn-small min-h-[36px] inline-flex items-center" href="{{ route('complaints.show',$complaint) }}">{{ __('common.view') }}</a></td></tr>@empty<tr><td colspan="10" class="empty">{{ __('common.no_complaints') }}</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $complaints->links() }}</div></div>
@endsection
