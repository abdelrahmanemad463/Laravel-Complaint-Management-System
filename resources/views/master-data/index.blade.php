@extends('layouts.app')
@section('content')
<div class="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p class="eyebrow">{{ __('common.configuration') }}</p><h1 class="page-title">{{ __('common.' . $type) }}</h1></div><a class="btn-primary" href="{{ route('master.create',$type) }}">{{ __('common.new') }}</a></div>
<div class="mb-6 flex flex-wrap gap-2">@foreach(['branches'=>'common.branches','services'=>'common.services','sources'=>'common.sources','categories'=>'common.categories','types'=>'common.types','priorities'=>'common.priorities','statuses'=>'common.statuses'] as $key=>$label)<a class="btn-small {{ $type===$key?'bg-indigo-100 text-indigo-700':'' }}" href="{{ route('master.index',$key) }}">{{ __($label) }}</a>@endforeach</div>
<div class="card overflow-hidden p-0"><div class="overflow-x-auto"><table class="data-table"><thead><tr><th>{{ __('common.name') }}</th><th>{{ __('common.color') }}</th><th>{{ __('common.active') }}</th><th>{{ __('common.order') }}</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td class="font-semibold">{{ $item->name }}</td><td>@if($item->color)<span class="inline-block h-5 w-5 rounded-full border" style="background:{{ $item->color }}"></span> {{ $item->color }}@else—@endif</td><td>{{ $item->is_active ? __('common.yes') : __('common.no') }}</td><td>{{ $item->sort_order }}</td><td class="text-end"><a class="btn-small" href="{{ route('master.edit',[$type,$item->id]) }}">{{ __('common.edit') }}</a></td></tr>@empty<tr><td colspan="5" class="empty">{{ __('common.no_data') }}</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $items->links() }}</div></div>
@endsection
